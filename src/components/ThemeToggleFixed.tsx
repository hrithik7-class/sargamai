"use client";

/** Theme toggle is now integrated into Navbar (desktop) and Dashboard layout. This component is kept for any pages that might need a standalone toggle but currently renders nothing. */
export default function ThemeToggleFixed() {
  return null; // Toggle moved to Navbar and Dashboard header
}
